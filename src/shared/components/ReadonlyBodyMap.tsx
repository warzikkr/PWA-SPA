/**
 * DEPRECATED — kept for backward compatibility.
 * Use UnifiedBodyMap with mode="readonly" instead.
 */
export { UnifiedBodyMap as ReadonlyBodyMap } from '../../components/bodymap/UnifiedBodyMap';
